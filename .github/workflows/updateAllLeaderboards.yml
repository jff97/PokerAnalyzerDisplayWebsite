name: Update Static Cached Leaderboards

permissions:
  contents: write

on:
  repository_dispatch:
    types: [refresh_leaderboards]
  workflow_dispatch:

jobs:
  update-leaderboards:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Make Sure API is Reachable
        run: |
          for i in {1..3}; do
            if [ $(curl -s -o /dev/null -w "%{http_code}" "https://api.johnfoxweb.com") -eq 200 ]; then
              exit 0
            else
              sleep 40
            fi
          done
          exit 1
      
      - name: Create cache directory
        run: mkdir -p static/cachedLeaderboards
      
      - name: Fetch trueskill data
        run: curl -s "https://api.johnfoxweb.com/api/leaderboard/trueskill" > "static/cachedLeaderboards/trueskill.json"

      - name: Fetch players outlasted data
        run: curl -s "https://api.johnfoxweb.com/api/leaderboard/players-outlasted" > "static/cachedLeaderboards/players-outlasted.json"

      - name: Fetch ROI data
        run: curl -s "https://api.johnfoxweb.com/api/leaderboard/roi" > "static/cachedLeaderboards/roi.json"

      - name: Fetch ROI low round requirement data
        run: curl -s "https://api.johnfoxweb.com/api/leaderboard/roi?min_rounds=3" > "static/cachedLeaderboards/roi-low-round-requirement.json"

      - name: Fetch first place data
        run: curl -s "https://api.johnfoxweb.com/api/leaderboard/firstplace" > "static/cachedLeaderboards/firstplace.json"

      - name: Fetch ITM percent data
        run: curl -s "https://api.johnfoxweb.com/api/leaderboard/itmpercent" > "static/cachedLeaderboards/itmpercent.json"

      - name: Fetch placement distribution data
        run: curl -s "https://api.johnfoxweb.com/api/leaderboard/placement-distributions" > "static/cachedLeaderboards/placement-distribution.json"
      
      - name: Fetch tournament qualifiers data
        run: curl -s "https://api.johnfoxweb.com/api/qualification/tournament-qualifiers" > "static/cachedLeaderboards/tournament-qualifiers.json"
      
      #- name: Fetch community disconnectedness data
      #  run: curl -s "https://api.johnfoxweb.com/api/leaderboard/community-disconnectedness" > "static/cachedLeaderboards/community-disconnectedness.json"
      
      - name: Fetch monthly top points data
        run: curl -s "https://api.johnfoxweb.com/api/leaderboard/this-months-top-point-players" > "static/cachedLeaderboards/months-top-point-players.json"
      
      - name: Fetch yearly top points data
        run: |
          TODAY_YEAR=$(date +'%Y')
          echo "Year: $TODAY_YEAR"
          RESPONSE=$(curl -s -w "\n%{http_code}" "https://api.johnfoxweb.com/api/leaderboard/years-top-point-players?year=$TODAY_YEAR")
          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | sed '$d')
          echo "HTTP Status: $HTTP_CODE"
          if [ "$HTTP_CODE" != "200" ]; then
            echo "Error response: $BODY"
            exit 1
          fi
          echo "$BODY" > "static/cachedLeaderboards/years-top-point-players.json"
      
      - name: Fetch automatic points bars data
        run: curl -s "https://api.johnfoxweb.com/api/automatic-points/bars" > "static/cachedLeaderboards/automatic-points-bars.json"
      
      - name: Fetch all player names data
        run: curl -s "https://api.johnfoxweb.com/api/nametools/getallplayernames" > "static/cachedLeaderboards/player-names.json"
      
      - name: Fetch average opponent skill data
        run: curl -s "https://api.johnfoxweb.com/api/leaderboard/average-opponent-skill" > "static/cachedLeaderboards/average-opponent-skill.json"
      
      - name: Fetch average game size by player data
        run: curl -s "https://api.johnfoxweb.com/api/leaderboard/average-game-size-by-player" > "static/cachedLeaderboards/average-game-size-by-player.json"
      
      - name: Fetch bar average players data
        run: curl -s "https://api.johnfoxweb.com/api/leaderboard/bar-average-players" > "static/cachedLeaderboards/bar-average-players.json"
      
      - name: Fetch bar average trueskill data
        run: curl -s "https://api.johnfoxweb.com/api/leaderboard/bar-average-trueskill" > "static/cachedLeaderboards/bar-average-trueskill.json"
      
      - name: Commit and Push
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          git add static/cachedLeaderboards/*.json
          git commit -m "Update all leaderboard data" || exit 0
          git push